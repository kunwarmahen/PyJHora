[pytest]
# Golden-value + endpoint smoke tests for the web backend (§3.2).
# Run from web/backend:  python -m pytest   (or ../dev.sh test from web/)
testpaths = tests
python_files = test_*.py
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
